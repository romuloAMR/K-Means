[project]
name = "k-means"
version = "0.1.0"
description = "Implementation and optimisation of the K-Means algorithm in Java and Go language"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "pandas>=3.0.2",
]
