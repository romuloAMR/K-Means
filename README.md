# K-Means
Implementation and optimisation of the K-Means algorithm in Java and Go language.

## Version legend:
- **V1:** Full serial version of the algorithm (base)
- **V2:** Version with platform threads + batch for reading the CSV file and executing the algorithm and no thread safety
- **V3:** Version with virtual threads + batch for reading the CSV file and executing the algorithm and no thread safety
